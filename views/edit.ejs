<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        main {
            display: flex;
            justify-content: center;
            align-items: center;
            flex: 1;
        }
    </style>
</head>
<body class="bg-body-tertiary">
    <main class="form-signin w-100 m-auto">
        <form action="/edit/<%= index %>" method="POST">
            <h1 class="h3 mb-3 fw-normal">Edit Post</h1>
            <div class="form-floating">
                <input type="text" class="form-control" id="floatingInput" name="title" placeholder="Title Name" value="<%= post.title %>" required>
                <label for="floatingInput">Title Name</label>
            </div>
            <div class="form-floating">
                <textarea class="form-control" id="floatingContent" name="content" placeholder="Enter the Blog" required><%= post.content %></textarea>
                <label for="floatingContent">Content</label>
            </div>
            <button class="btn btn-primary w-100 py-2" type="submit">Update Post</button>
        </form>
    </main>
</body>
</html>
